# Public production origin used for canonical, sitemap, and robots URLs.
# Vercel supplies VERCEL_PROJECT_PRODUCTION_URL automatically, so this is optional there.
NEXT_PUBLIC_SITE_URL=https://your-domain.example
